package com.konrad.brk.KFiszki;

import com.konrad.brk.KFiszki.model.Role;
import com.konrad.brk.KFiszki.repository.RoleRepository;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class KFiszkiApplication {
	public static void main(String[] args) {
		SpringApplication.run(KFiszkiApplication.class, args);
	}

}
